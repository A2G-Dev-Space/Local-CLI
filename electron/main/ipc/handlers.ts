/**
 * IPC Handlers Export
 *
 * Re-exports from ipc-handlers.ts for new structure compatibility
 */

export {
  setupIpcHandlers,
  cleanupIpcHandlers,
  setChatWindow,
  setTaskWindow,
} from '../ipc-handlers';
