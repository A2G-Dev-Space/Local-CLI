/**
 * Tool Registry Export
 *
 * Re-exports from agent/tools/registry for backward compatibility
 */

export { toolRegistry } from '../agent/tools/registry';
export type { OptionalToolGroup, EnableResult } from '../agent/tools/registry';
