/**
 * A2G-CLI Main Export
 *
 * 이 파일은 라이브러리로 사용될 때의 진입점입니다.
 * CLI 실행은 src/cli.ts를 사용합니다.
 */

export const version = '0.1.0';
export const name = 'a2g-cli';

/**
 * 추후 API로 사용될 함수들을 여기에 export합니다.
 */

// Placeholder for future exports
export {};
